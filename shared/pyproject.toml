[project]
name = "memoryvault-shared"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["sqlalchemy>=2.0.49,<3.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
